# Отчет о реализованных API

## ERPMax Orchestrator - SaaS платформа

**Дата:** 9 января 2026  
**Версия:** 0.1.0  
**Технологический стек:** FastAPI, PostgreSQL, Redis, Socket.IO

---

## 1. Обзор системы

ERPMax Orchestrator — это backend-платформа для управления SaaS-системой ERPMax с поддержкой multi-tenancy, аутентификации, биллинга и интеграции с ERPNext.

### Основные возможности

- ✅ Multi-tenancy архитектура
- ✅ JWT аутентификация и авторизация
- ✅ Управление подписками и тарифными планами
- ✅ Интеграция с платежными системами (Stripe)
- ✅ Provisioning инфраструктуры
- ✅ SSO интеграция с ERPNext
- ✅ Real-time уведомления через WebSocket

---

## 2. REST API Endpoints

### 2.1 Аутентификация (`/api/v1/auth`)

#### **POST /api/v1/auth/register**

Регистрация нового пользователя с созданием компании

**Функционал:**

- Создание учетной записи пользователя
- Автоматическое создание Tenant (компании)
- Назначение роли Owner
- Создание пробной подписки (Trial) на 14 дней
- Возврат JWT токенов (access + refresh)

**Request Body:**

```json
{
  "email": "user@example.com",
  "password": "SecurePass123!",
  "full_name": "John Doe",
  "company_name": "Acme Corp"
}
```

**Response:** `201 Created`

```json
{
  "status": "success",
  "data": {
    "access_token": "eyJ...",
    "refresh_token": "eyJ...",
    "token_type": "bearer",
    "user": {
      "id": "uuid",
      "email": "user@example.com",
      "full_name": "John Doe"
    },
    "tenants": [...],
    "current_tenant": {...}
  }
}
```

---

#### **POST /api/v1/auth/login**

Аутентификация пользователя

**Функционал:**

- Проверка учетных данных
- Возврат JWT токенов
- Информация о пользователе и всех его Tenants
- Текущий/дефолтный Tenant

**Request Body:**

```json
{
  "email": "user@example.com",
  "password": "SecurePass123!"
}
```

**Response:** `200 OK`

```json
{
  "status": "success",
  "data": {
    "access_token": "eyJ...",
    "refresh_token": "eyJ...",
    "token_type": "bearer",
    "user": {...},
    "tenants": [...],
    "current_tenant": {...}
  }
}
```

---

#### **POST /api/v1/auth/refresh**

Обновление access токена

**Функционал:**

- Валидация refresh токена
- Выдача новых access и refresh токенов

**Request Body:**

```json
{
  "refresh_token": "eyJ..."
}
```

**Response:** `200 OK`

---

#### **POST /api/v1/auth/logout**

Выход из системы

**Требования:** Bearer Token  
**Response:** `200 OK`

> **Примечание:** В текущей версии возвращает успешный ответ. Планируется добавление blacklist токенов в Redis.

---

#### **GET /api/v1/auth/me**

Получение информации о текущем пользователе

**Требования:** Bearer Token  
**Функционал:**

- Информация о пользователе
- Список всех Tenants с ролями
- Текущий Tenant

**Response:** `200 OK`

---

#### **POST /api/v1/auth/switch-tenant**

Переключение между компаниями (Tenants)

**Требования:** Bearer Token  
**Функционал:**

- Проверка доступа к Tenant
- Обновление дефолтного Tenant
- Выдача новых JWT токенов с новым tenant_id

**Request Body:**

```json
{
  "tenant_id": "uuid"
}
```

**Response:** `200 OK`

---

### 2.2 Управление Tenants (`/api/v1/tenants`)

#### **GET /api/v1/tenants**

Список всех компаний пользователя

**Требования:** Bearer Token  
**Функционал:**

- Пагинация
- Включает роль пользователя в каждом Tenant

**Query Parameters:**

- `page` (default: 1)
- `size` (default: 10)

**Response:** `200 OK`

---

#### **GET /api/v1/tenants/current**

Информация о текущем Tenant

**Требования:** Bearer Token  
**Функционал:**

- Детали компании
- Информация о подписке
- Лимиты из тарифного плана

**Response:** `200 OK`

---

#### **GET /api/v1/tenants/{tenant_id}**

Получение конкретного Tenant по ID

**Требования:** Bearer Token, доступ к Tenant  
**Response:** `200 OK`

---

#### **PATCH /api/v1/tenants/{tenant_id}**

Обновление информации о Tenant

**Требования:** Bearer Token, роль Owner или Admin  
**Функционал:**

- Изменение названия
- Обновление настроек

**Request Body:**

```json
{
  "name": "New Company Name",
  "settings": {...}
}
```

**Response:** `200 OK`

---

#### **GET /api/v1/tenants/{tenant_id}/users**

Список пользователей в Tenant

**Требования:** Bearer Token, роль Owner или Admin  
**Функционал:**

- Пагинация
- Информация о ролях пользователей

**Response:** `200 OK`

---

#### **POST /api/v1/tenants/{tenant_id}/users/invite**

Приглашение пользователя в Tenant

**Требования:** Bearer Token, роль Owner или Admin  
**Функционал:**

- Пользователь должен существовать в системе
- Отправка email-приглашения (TODO)

**Request Body:**

```json
{
  "email": "newuser@example.com",
  "role": "MEMBER"
}
```

**Response:** `201 Created`

---

#### **PATCH /api/v1/tenants/{tenant_id}/users/{user_id}**

Изменение роли пользователя

**Требования:** Bearer Token, роль Owner  
**Функционал:**

- Нельзя изменить роль Owner
- Доступные роли: OWNER, ADMIN, MEMBER

**Request Body:**

```json
{
  "role": "ADMIN"
}
```

**Response:** `200 OK`

---

#### **DELETE /api/v1/tenants/{tenant_id}/users/{user_id}**

Удаление пользователя из Tenant

**Требования:** Bearer Token, роль Owner или Admin  
**Ограничения:**

- Нельзя удалить Owner
- Нельзя удалить самого себя

**Response:** `200 OK`

---

### 2.3 Биллинг и подписки (`/api/v1/billing`)

#### **GET /api/v1/billing/plans**

Список доступных тарифных планов

**Функционал:**

- Только активные планы
- Информация о ценах и лимитах

**Response:** `200 OK`

```json
{
  "status": "success",
  "data": [
    {
      "id": "uuid",
      "name": "Professional",
      "description": "For growing businesses",
      "price_monthly": 99.00,
      "price_yearly": 990.00,
      "features": {...},
      "limits": {
        "max_users": 10,
        "max_storage_gb": 100
      }
    }
  ]
}
```

---

#### **GET /api/v1/billing/subscription**

Текущая подписка Tenant

**Требования:** Bearer Token  
**Функционал:**

- Информация о подписке
- Детали тарифного плана
- Статус и даты

**Response:** `200 OK`

---

#### **POST /api/v1/billing/checkout**

Создание сессии оплаты

**Требования:** Bearer Token  
**Функционал:**

- Интеграция со Stripe
- Создание checkout сессии для смены плана
- Поддержка monthly/yearly периодов

**Request Body:**

```json
{
  "plan_id": "uuid",
  "billing_period": "monthly",
  "payment_provider": "stripe",
  "success_url": "https://app.example.com/success",
  "cancel_url": "https://app.example.com/cancel"
}
```

**Response:** `200 OK`

```json
{
  "status": "success",
  "data": {
    "checkout_url": "https://checkout.stripe.com/...",
    "session_id": "cs_..."
  }
}
```

---

#### **POST /api/v1/billing/cancel**

Отмена подписки

**Требования:** Bearer Token, роль Owner  
**Функционал:**

- Немедленная отмена или в конце периода
- Указание причины отмены

**Request Body:**

```json
{
  "reason": "Too expensive",
  "cancel_at_period_end": true
}
```

**Response:** `200 OK`

---

#### **GET /api/v1/billing/invoices**

История платежей

**Требования:** Bearer Token  
**Функционал:**

- Пагинация
- Список всех платежных событий

**Response:** `200 OK`

---

#### **POST /api/v1/billing/webhook/stripe**

Webhook для обработки событий Stripe

**Функционал:**

- Валидация подписи Stripe
- Обработка событий: payment_succeeded, subscription_updated, и др.
- Автоматическое обновление статусов подписок

**Headers:**

- `Stripe-Signature`: webhook signature

**Response:** `200 OK`

---

### 2.4 Provisioning (`/api/v1/provisioning`)

#### **POST /api/v1/provisioning/jobs**

Создание задачи provisioning

**Требования:** Bearer Token, роль Owner или Admin  
**Функционал:**

- Создание инфраструктуры для Tenant
- Асинхронное выполнение

**Request Body:**

```json
{
  "job_type": "create_instance"
}
```

**Response:** `201 Created`

---

#### **GET /api/v1/provisioning/jobs**

Список задач provisioning

**Требования:** Bearer Token  
**Функционал:**

- Пагинация
- Только задачи текущего Tenant

**Response:** `200 OK`

---

#### **GET /api/v1/provisioning/jobs/{job_id}**

Получение статуса задачи

**Требования:** Bearer Token  
**Функционал:**

- Детальная информация о задаче
- Статус выполнения
- Логи и ошибки

**Response:** `200 OK`

---

#### **POST /api/v1/provisioning/jobs/{job_id}/retry**

Повторный запуск задачи

**Требования:** Bearer Token, роль Owner или Admin  
**Response:** `200 OK`

---

#### **POST /api/v1/provisioning/jobs/{job_id}/cancel**

Отмена задачи

**Требования:** Bearer Token, роль Owner или Admin  
**Response:** `200 OK`

---

### 2.5 SSO интеграция (`/api/v1/sso`)

#### **POST /api/v1/sso/erpnext/token**

Генерация SSO токена для ERPNext

**Требования:** Bearer Token  
**Функционал:**

- Создание одноразового токена
- Срок действия: 60 секунд
- Токен удаляется после использования

**Response:** `200 OK`

```json
{
  "status": "success",
  "data": {
    "sso_url": "https://erpnext.example.com/sso/callback?token=...",
    "token": "abc123...",
    "expires_in": 60
  }
}
```

---

#### **GET /api/v1/sso/erpnext/callback**

Callback для валидации SSO токена

**Функционал:**

- Валидация токена
- Возврат данных сессии для ERPNext
- Автоматическое удаление токена

**Query Parameters:**

- `token`: SSO токен (обязательный)
- `redirect`: путь редиректа после входа (default: /desk)

**Response:** `200 OK`

```json
{
  "success": true,
  "data": {
    "user_id": "uuid",
    "tenant_id": "uuid",
    "email": "user@example.com",
    "full_name": "John Doe"
  },
  "redirect": "/desk"
}
```

---

#### **GET /api/v1/sso/erpnext/validate/{token}**

Проверка валидности токена без его использования

**Функционал:**

- Проверка существования токена
- НЕ удаляет токен из Redis

**Response:** `200 OK`

---

## 3. WebSocket API (Socket.IO)

### 3.1 Подключение

**Endpoint:** `ws://localhost:8000/ws`

**Аутентификация:**

```javascript
const socket = io('http://localhost:8000/ws', {
  auth: {
    token: 'your_jwt_access_token'
  }
});
```

**События при подключении:**

- Валидация JWT токена
- Автоматическое присоединение к комнатам:
  - `user:{user_id}` — персональные уведомления
  - `tenant:{tenant_id}` — уведомления компании

---

### 3.2 События

#### **connected**

Подтверждение успешного подключения

**Payload:**

```json
{
  "message": "Successfully connected to ERPMax Orchestrator",
  "user_id": "uuid",
  "tenant_id": "uuid"
}
```

---

#### **ping / pong**

Keepalive механизм

**Client → Server:** `ping`  
**Server → Client:** `pong`

---

### 3.3 Namespaces

#### **/notifications**

Namespace для уведомлений

**События:**

- `notification_read` — отметить уведомление как прочитанное
- `notification_read_confirmed` — подтверждение

**Использование:**

```javascript
const notifSocket = io('http://localhost:8000/ws/notifications', {
  auth: { token: 'jwt_token' }
});

notifSocket.emit('notification_read', {
  notification_id: 'uuid'
});
```

---

#### **/provisioning**

Namespace для событий provisioning

**Функционал:**

- Real-time обновления статуса задач
- Уведомления о завершении/ошибках

---

#### **/billing**

Namespace для биллинг событий

**Функционал:**

- Уведомления об успешных платежах
- Предупреждения об истечении подписки
- Изменения тарифного плана

---

## 4. Модели данных

### 4.1 Основные сущности

#### **User** (Пользователь)

- `id`: UUID
- `email`: уникальный email
- `password_hash`: хеш пароля
- `full_name`: полное имя
- `is_active`: статус активности
- `default_tenant_id`: дефолтный Tenant

#### **Tenant** (Компания)

- `id`: UUID
- `name`: название компании
- `slug`: уникальный URL-friendly идентификатор
- `settings`: JSON настройки
- `created_at`: дата создания

#### **TenantUser** (Связь пользователь-компания)

- `user_id`: UUID
- `tenant_id`: UUID
- `role`: OWNER | ADMIN | MEMBER
- `joined_at`: дата присоединения

#### **Plan** (Тарифный план)

- `id`: UUID
- `name`: название плана
- `description`: описание
- `price_monthly`: цена в месяц
- `price_yearly`: цена в год
- `features`: JSON характеристики
- `limits`: JSON лимиты

#### **Subscription** (Подписка)

- `id`: UUID
- `tenant_id`: UUID
- `plan_id`: UUID
- `status`: TRIAL | ACTIVE | CANCELLED | EXPIRED
- `billing_period`: MONTHLY | YEARLY
- `current_period_start`: начало периода
- `current_period_end`: конец периода
- `cancel_at_period_end`: флаг отмены

#### **PaymentEvent** (Платежное событие)

- `id`: UUID
- `subscription_id`: UUID
- `event_type`: PAYMENT_SUCCEEDED | PAYMENT_FAILED | REFUND
- `amount`: сумма
- `currency`: валюта
- `provider`: STRIPE | PAYPAL
- `provider_event_id`: ID события у провайдера

#### **ProvisioningJob** (Задача provisioning)

- `id`: UUID
- `tenant_id`: UUID
- `job_type`: CREATE_INSTANCE | UPDATE_INSTANCE | DELETE_INSTANCE
- `status`: PENDING | IN_PROGRESS | COMPLETED | FAILED
- `result`: JSON результат
- `error`: текст ошибки

---

## 5. Безопасность

### 5.1 Аутентификация

- **JWT токены** с access (15 мин) и refresh (7 дней) токенами
- **Bcrypt** для хеширования паролей
- **Bearer Token** аутентификация для API

### 5.2 Авторизация

- **Role-based access control (RBAC)**
- Роли: OWNER, ADMIN, MEMBER
- Проверка доступа на уровне endpoint

### 5.3 Валидация

- **Pydantic schemas** для валидации входных данных
- Проверка прав доступа к ресурсам Tenant
- CORS настройки для безопасности

---

## 6. Интеграции

### 6.1 Stripe

- Checkout сессии для оплаты
- Webhook обработка событий
- Автоматическое управление подписками

### 6.2 ERPNext

- SSO интеграция через одноразовые токены
- Автоматический вход без пароля
- Безопасная передача данных сессии

---

## 7. Технические характеристики

### 7.1 API

- **Формат:** REST API
- **Версионирование:** `/api/v1/`
- **Документация:** Swagger UI (`/docs`), ReDoc (`/redoc`)
- **Формат ответов:** JSON

### 7.2 Производительность

- **Async/await** для всех операций БД
- **Connection pooling** для PostgreSQL
- **Redis** для кеширования и сессий
- **Пагинация** для списочных запросов

### 7.3 Мониторинг

- Health check endpoint: `/health`
- Логирование всех операций
- Структурированные логи

---

## 8. Статус разработки

### ✅ Реализовано

- Полная система аутентификации и авторизации
- Multi-tenancy с управлением пользователями
- Биллинг система с интеграцией Stripe
- Provisioning инфраструктуры
- SSO интеграция с ERPNext
- WebSocket real-time коммуникация
- База данных с миграциями

### 🚧 В разработке

- Email уведомления
- Blacklist токенов в Redis
- Расширенная аналитика
- Дополнительные платежные провайдеры

### 📋 Планируется (Phase 2)

- Восстановление пароля (`/forgot-password`, `/reset-password`)
- Двухфакторная аутентификация (2FA)
- Audit logs
- Advanced reporting

---

## 9. Примеры использования

### 9.1 Регистрация и вход

```bash
# Регистрация
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "john@example.com",
    "password": "SecurePass123!",
    "full_name": "John Doe",
    "company_name": "Acme Corp"
  }'

# Вход
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "john@example.com",
    "password": "SecurePass123!"
  }'
```

### 9.2 Работа с API

```bash
# Получить текущего пользователя
curl -X GET http://localhost:8000/api/v1/auth/me \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"

# Список тарифных планов
curl -X GET http://localhost:8000/api/v1/billing/plans

# Создать checkout сессию
curl -X POST http://localhost:8000/api/v1/billing/checkout \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "plan_id": "uuid",
    "billing_period": "monthly",
    "payment_provider": "stripe",
    "success_url": "https://app.example.com/success",
    "cancel_url": "https://app.example.com/cancel"
  }'
```

### 9.3 WebSocket подключение

```javascript
// Подключение
const socket = io('http://localhost:8000/ws', {
  auth: {
    token: accessToken
  }
});

// Обработка событий
socket.on('connected', (data) => {
  console.log('Connected:', data);
});

socket.on('notification', (data) => {
  console.log('New notification:', data);
});

// Keepalive
setInterval(() => {
  socket.emit('ping');
}, 30000);
```

---

## 10. Документация проекта

В проекте подготовлена полная техническая документация, охватывающая все аспекты системы:

### 10.1 Основная документация

#### **README.md**

Главный файл документации с информацией о:

- Технологическом стеке (Python 3.12+, FastAPI, PostgreSQL, Redis, RabbitMQ)
- Быстром старте и установке
- Структуре проекта
- Командах для работы с миграциями
- Docker конфигурации

#### **DEPLOYMENT_GUIDE.md** (`/docs`)

Полное руководство по развертыванию:

- Стратегия миграций базы данных
- Архитектура deployment с отдельным migration service
- Инструкции для локальной разработки и production
- Zero-downtime deployment стратегии
- Мониторинг миграций
- Best practices и troubleshooting
- CI/CD интеграция

#### **DEPLOY_INSTRUCTIONS.md**

Практическое руководство по развертыванию:

- Пошаговые инструкции первого развертывания
- Процедура обновления приложения
- Полезные команды для управления сервером
- Backup и восстановление базы данных
- Настройка production окружения
- Troubleshooting распространенных проблем

### 10.2 Специализированная документация

#### **BILLING.md** (`/docs`)

Документация биллинг-системы:

- Архитектура подписок и тарифных планов
- Интеграция с платежными провайдерами (Stripe, LiqPay)
- Обработка webhook событий
- Управление жизненным циклом подписок

#### **SSO.md** (`/docs`)

Документация SSO интеграции:

- Архитектура Single Sign-On с ERPNext
- Генерация и валидация одноразовых токенов
- Безопасность и срок действия токенов
- Примеры использования API

#### **PROVISIONING.md** (`/docs`)

Документация системы provisioning:

- Архитектура асинхронных задач
- Типы provisioning jobs
- Мониторинг и управление задачами
- Интеграция с RabbitMQ

#### **REALTIME.md** (`/docs`)

Документация WebSocket API:

- Архитектура Socket.IO сервера
- Namespaces и события
- Аутентификация через JWT
- Примеры клиентского кода

#### **CORS.md** (`/docs`)

Настройка CORS для безопасности:

- Конфигурация разрешенных origins
- Настройка для разных окружений
- Best practices

#### **MIGRATION_STRATEGY.md** (`/docs`)

Стратегия работы с миграциями:

- Подход к созданию миграций
- Тестирование миграций
- Rollback процедуры
- Production deployment

### 10.3 Вспомогательная документация

#### **QUICK_COMMANDS.md**

Справочник быстрых команд:

- Команды развертывания
- SSH команды для управления
- Просмотр логов
- Backup и восстановление

#### **PROJECT_STATUS.md**

Текущий статус проекта:

- Реализованные фичи
- В разработке
- Планируемые улучшения

#### **TEST_USERS.md** (`/docs`)

Тестовые пользователи для разработки

---

## 11. Средства развертывания

Проект включает полный набор инструментов для автоматизированного развертывания на production серверах.

### 11.1 Docker конфигурация

#### **Dockerfile**

Оптимизированный multi-stage образ:

```dockerfile
FROM python:3.12-slim
- Установка системных зависимостей (gcc, netcat)
- Копирование и установка Python пакетов
- Настройка entrypoint скрипта
- Expose порта 8000
```

**Особенности:**

- Минимальный размер образа (slim базовый образ)
- Кеширование слоев для быстрой пересборки
- Health checks для мониторинга

#### **docker-compose.yml**

Конфигурация для локальной разработки:

- **PostgreSQL 16** с persistent volumes
- **Redis 7** для кеширования
- **RabbitMQ 3** с management UI
- Health checks для всех сервисов
- Опциональный app контейнер

**Порты:**

- PostgreSQL: 5432
- Redis: 6379
- RabbitMQ: 5672 (AMQP), 15672 (Management UI)
- API: 8000

#### **docker-compose.prod.yml**

Production-ready конфигурация:

**Сервисы:**

1. **postgres** - База данных
   - Restart policy: unless-stopped
   - Health checks
   - Persistent volumes
   - Изолированная сеть

2. **redis** - Кеш и сессии
   - Защита паролем
   - Persistent volumes
   - Health checks

3. **rabbitmq** - Очередь задач
   - Management UI
   - Persistent volumes
   - Health checks

4. **migration** - Сервис миграций
   - Запускается один раз перед app
   - Выполняет `alembic upgrade head`
   - Restart: no (завершается после выполнения)

5. **app** - Основное приложение
   - 4 worker процесса (uvicorn)
   - Зависит от успешного завершения migration
   - Restart: unless-stopped
   - Health checks

6. **worker** - Background tasks
   - Обработка асинхронных задач
   - Зависит от migration
   - Restart: unless-stopped

**Особенности:**

- Dependency management (app запускается только после migration)
- Network isolation
- Environment variables из `.env.production`
- Persistent volumes для данных
- Автоматический restart при сбоях

### 11.2 Скрипты развертывания

#### **deploy.sh**

Автоматизированный скрипт первого развертывания:

**Функционал:**

1. ✅ Проверка SSH соединения с сервером
2. ✅ Установка Docker и Docker Compose (если нужно)
3. ✅ Создание директории проекта на сервере
4. ✅ Копирование файлов через rsync
5. ✅ Генерация безопасных паролей и SECRET_KEY
6. ✅ Создание `.env.production` файла
7. ✅ Остановка существующих контейнеров
8. ✅ Сборка Docker образов
9. ✅ Запуск всех сервисов
10. ✅ Проверка миграций
11. ✅ Health check API
12. ✅ Вывод информации о доступе

**Использование:**

```bash
chmod +x deploy.sh
./deploy.sh
```

**Автоматическая генерация:**

- PostgreSQL пароль (32 символа)
- Redis пароль (32 символа)
- RabbitMQ пароль (32 символа)
- SECRET_KEY (64 hex символа)

#### **update.sh**

Скрипт обновления развернутого приложения:

**Процесс обновления:**

1. ✅ Копирование обновленных файлов
2. ✅ Остановка app и worker (БД продолжает работать)
3. ✅ Пересборка Docker образов
4. ✅ Выполнение миграций
5. ✅ Запуск обновленных сервисов
6. ✅ Health check
7. ✅ Проверка статуса

**Использование:**

```bash
chmod +x update.sh
./update.sh
```

**Преимущества:**

- Минимальный downtime
- Безопасное обновление БД
- Автоматический rollback при ошибках
- Проверка работоспособности

#### **docker-entrypoint.sh**

Entrypoint скрипт для контейнеров:

**Функции:**

- Ожидание готовности PostgreSQL
- Опциональный запуск миграций (если `RUN_MIGRATIONS=true`)
- Запуск основного приложения
- Обработка сигналов для graceful shutdown

### 11.3 Environment конфигурация

#### **.env.example**

Шаблон для локальной разработки:

- Настройки БД
- Redis и RabbitMQ
- Базовые параметры приложения

#### **.env.production.example**

Шаблон для production:

- Обязательные переменные с валидацией
- Настройки безопасности
- Интеграции (Stripe, LiqPay, ERPNext)
- CORS настройки

**Обязательные переменные:**

```bash
POSTGRES_PASSWORD=<generated>
REDIS_PASSWORD=<generated>
RABBITMQ_PASSWORD=<generated>
SECRET_KEY=<generated>
```

**Опциональные настройки:**

```bash
ALLOWED_ORIGINS=https://yourdomain.com
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
LIQPAY_PUBLIC_KEY=...
LIQPAY_PRIVATE_KEY=...
ERPNEXT_BASE_URL=https://erpnext.yourdomain.com
```

### 11.4 Стратегия миграций

**Архитектура:**

```
PostgreSQL (healthy)
    ↓
Migration Container (runs once)
    ↓
App Container (starts after migration success)
```

**Преимущества:**

- ✅ Миграции выполняются один раз
- ✅ Нет конфликтов при масштабировании
- ✅ App не запустится с несовместимой схемой БД
- ✅ Простой rollback
- ✅ Логирование миграций

**Команды:**

```bash
# Проверка текущей версии
docker compose -f docker-compose.prod.yml run --rm migration alembic current

# История миграций
docker compose -f docker-compose.prod.yml run --rm migration alembic history

# Rollback
docker compose -f docker-compose.prod.yml run --rm migration alembic downgrade -1
```

### 11.5 Мониторинг и управление

#### **Health Checks**

Все сервисы имеют health checks:

- PostgreSQL: `pg_isready`
- Redis: `redis-cli ping`
- RabbitMQ: `rabbitmq-diagnostics ping`
- API: `GET /health`

#### **Логирование**

```bash
# Все сервисы
docker compose -f docker-compose.prod.yml logs -f

# Конкретный сервис
docker logs -f erpmax_orchestrator
docker logs erpmax_migration
docker logs erpmax_postgres
```

#### **Статус сервисов**

```bash
docker compose -f docker-compose.prod.yml ps
docker stats --no-stream
```

#### **Backup базы данных**

```bash
# Создание backup
docker exec erpmax_postgres pg_dump -U erpmax erpmax_orchestrator > backup.sql

# Восстановление
docker exec -i erpmax_postgres psql -U erpmax erpmax_orchestrator < backup.sql
```

### 11.6 Безопасность deployment

**Реализованные меры:**

- ✅ Автоматическая генерация сложных паролей
- ✅ Изоляция сервисов в Docker network
- ✅ Переменные окружения не хранятся в git
- ✅ Health checks для мониторинга
- ✅ Restart policies для отказоустойчивости
- ✅ Persistent volumes для сохранности данных

**Рекомендации:**

- Настроить firewall (UFW)
- Использовать SSL/TLS (Nginx/Caddy reverse proxy)
- Регулярные backup базы данных
- Мониторинг ресурсов сервера
- Обновление Docker образов

### 11.7 CI/CD готовность

Проект готов к интеграции с CI/CD системами:

**GitHub Actions пример:**

```yaml
name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build images
        run: docker compose -f docker-compose.prod.yml build
      - name: Run migrations
        run: docker compose -f docker-compose.prod.yml up migration
      - name: Deploy app
        run: docker compose -f docker-compose.prod.yml up -d app worker
```

**Поддерживаемые платформы:**

- GitHub Actions
- GitLab CI/CD
- Jenkins
- CircleCI
- Любая система с Docker support

---

## 12. Заключение

ERPMax Orchestrator представляет собой полнофункциональную backend-платформу для управления SaaS-системой с поддержкой:

- ✅ **25+ REST API endpoints** для всех основных операций
- ✅ **WebSocket API** для real-time коммуникации
- ✅ **Multi-tenancy** архитектура
- ✅ **Интеграция с платежными системами**
- ✅ **SSO для бесшовной интеграции с ERPNext**
- ✅ **Масштабируемая и безопасная архитектура**
- ✅ **Полная документация** по всем аспектам системы
- ✅ **Автоматизированное развертывание** с помощью скриптов
- ✅ **Production-ready** Docker конфигурация
- ✅ **Стратегия миграций** для безопасного обновления

### Готовность к production

**Инфраструктура:**

- Docker и Docker Compose конфигурация
- Автоматические миграции базы данных
- Health checks и мониторинг
- Backup и восстановление

**Документация:**

- 10+ документов с детальным описанием
- Примеры использования API
- Руководства по развертыванию
- Troubleshooting гайды

**Автоматизация:**

- Скрипты развертывания (deploy.sh)
- Скрипты обновления (update.sh)
- CI/CD готовность
- Автоматическая генерация паролей

Система готова к немедленному развертыванию на production серверах и дальнейшему расширению функционала.

---

**Контакты для вопросов:**  
Техническая документация: `/docs`  
API Reference: `/redoc`  
Swagger UI: `/docs`
