[project]
name = "erpmax-orchestrator"
version = "0.1.0"
description = "ERPMax SaaS Orchestrator - FastAPI Backend"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py312"
